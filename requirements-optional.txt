# Необязательные ускорители. Приложение работает и без них.
#
# Установка:
#   py -3.14 -m pip install -r requirements-optional.txt
#
# Если что-то отсюда не встанет — ничего страшного, но ставьте этот файл
# отдельно от основного: pip прерывает установку целиком на первом же
# пакете, который не собрался, и вместе с ускорителем вы теряете нужное.

# Быстрый AES-CTR для MTProxy, написанный на C под протокол Telegram.
#
# Готовых колёс под Python 3.14 нет — понадобится компилятор C
# (Visual Studio Build Tools). Без него установка упадёт, и это
# нормально: шифрование возьмёт на себя cryptography, просто медленнее.
TgCrypto==1.2.5
